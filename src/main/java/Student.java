public class Student extends Scholar {

    /**
     * Create Student object. It inherits the Scholar class
     * @param id
     * @param name
     * @param course
     */
    public Student(String id, String name, String course) {
        super(id, name, course);
    }
}
